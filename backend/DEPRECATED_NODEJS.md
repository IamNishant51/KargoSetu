# DEPRECATED: Node.js Backend

The following files are part of the original Node.js/Express backend and are
being replaced by the Python/FastAPI backend in `backend/app/`.

DO NOT modify these files. They are retained as reference only.

Deprecated files:
- index.js
- routes/*.js
- services/*.js
- middleware/*.js
- lib/*.js
- package.json
- package-lock.json
