delete Heavy ML Predictor with deep learning and live financial data downloads that just produces a toy 3-line graph and falls back to random noise. A simple math/heuristic function returning deterministic bounds. [backend/app/services/ml_predictor.py]
delete Unused dependencies included solely for the over-engineered ML predictor. Remove tensorflow, yfinance, pandas, scikit-learn, numpy from requirements. [backend/requirements.txt]
delete Dead code component `ForecastPriceChart.tsx` and its unused state store `marketStore.ts`. Remove files. [frontend/src/components/ForecastPriceChart.tsx, frontend/src/store/marketStore.ts]
delete Unfinished empty API router and a frontend ticker component that calls it, which only ever returns 404s. Remove files. [backend/app/api/routers/market.py, frontend/src/components/landing/MarketTicker.tsx]
shrink Context provider used for a single boolean state. Lift `useState` to parent `DashboardLayout` and pass as props. [frontend/src/app/dashboard/components/SidebarContext.tsx]
native `axios` HTTP client used for basic GET/POST/DELETE requests. Native Next.js `fetch` API. [frontend/package.json]
delete Unused state management library `zustand` added for a single variable in dead code. Remove from package.json. [frontend/package.json]
delete Empty, completely unused core modules. Remove files. [backend/app/core/config.py, backend/app/core/exceptions.py]

Net lines saved: 489 lines
Net deps saved: 7 dependencies


---

### Potential Judge Questions

Based on the provided documentation excerpt, here are 10 thoughtful and probing questions I would ask the team during their presentation:

1. You replaced a deep learning ML predictor with a "simple math/heuristic function returning deterministic bounds." How did you validate that this heuristic provides sufficient accuracy and value for your problem statement compared to an actual machine learning model?
2. The report notes that the original ML implementation "falls back to random noise." Was the original intention of your project to rely heavily on AI/ML, and did you pivot away from it due to implementation difficulties during the hackathon, or was it simply over-engineered for the core problem?
3. By removing `tensorflow`, `pandas`, `scikit-learn`, `numpy`, and `yfinance`, you saved significant overhead. How did this massive reduction in your backend's dependency footprint impact your deployment times, container sizes, and server resource constraints?
4. You removed the `market.py` API router and `MarketTicker.tsx` because they were unfinished and returning 404s. Were market tickers originally a critical requirement for your solution, and how does dropping this feature impact the overall completeness of your product?
5. You migrated away from `axios` to use the native Next.js `fetch` API for GET/POST/DELETE requests. How did you handle edge cases that `axios` typically manages out-of-the-box, such as interceptors, automatic JSON transformation, or timeout handling?
6. You refactored a Context provider used for a single boolean by lifting the `useState` hook to the parent `DashboardLayout`. While this removes unnecessary boilerplate, how will this prop-drilling approach scale if your dashboard UI complexity and state requirements grow in the future?
7. With the removal of `ForecastPriceChart.tsx` and its associated `marketStore.ts`, how are users currently visualizing the forecasting data generated by your new deterministic math function, or was visual forecasting dropped from the user interface entirely?
8. Removing `zustand` because it was tied to dead code is a great optimization. Did the team initially struggle with architectural planning, leading to the inclusion of heavy state-management libraries before realizing native React state was sufficient?
9. You deleted `config.py` and `exceptions.py` from your core backend modules because they were completely empty. Now that these dedicated modules are gone, how are you currently managing environment variables, configurations, and custom error handling in your backend?
10. Your cleanup phase resulted in a net savings of 489 lines of code and 7 dependencies, mostly by pruning "over-engineered" components. What was the turning point during the hackathon that made the team prioritize code simplicity and reliability over presenting complex buzzwords like "deep learning" to the judges?
